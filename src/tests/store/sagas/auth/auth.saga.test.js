import { loginUserSaga } from '../../../../store/sagas/auth/auth.saga';
import Item from 'antd/lib/list/Item';


describe('Sagas', () => {
    describe('Auth', () => {
        // const auth.saveCookieData = jest.fn();
        // selectors.isAuthenticated = jest.fn();

        beforeEach(() => {
            jest.resetAllMocks();
        });

        xit('Must do something', () => {

        })
    })
})